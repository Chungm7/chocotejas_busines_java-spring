spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.sql.init.mode=always

# Directorios de archivos (dummy values for tests)
file.upload-dir=./target/imagenes/
file.logo-dir=./target/logos/
file.slider-dir=./target/sliders/
file.momentos-dir=./target/momentos/

# MiAPICLoud API Token (dummy value for tests)
api.miapicloud.token=DUMMY_TOKEN
